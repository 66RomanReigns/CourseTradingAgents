services:
  tradinglab:
    build: .
    image: tradinglab-agent:course
    ports:
      - "8000:8000"
    environment:
      TRADINGLAB_ROOT: /app
    volumes:
      - ./artifacts:/app/artifacts
      - ./data:/app/data:ro
    restart: unless-stopped
